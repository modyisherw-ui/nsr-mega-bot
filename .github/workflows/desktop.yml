name: Build Desktop App

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "desktop/**"
      - ".github/workflows/desktop.yml"

permissions:
  contents: write
  issues: write

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: desktop/package-lock.json

      - name: Install deps
        working-directory: desktop
        run: npm ci

      - name: Enable developer mode (symlinks for winCodeSign)
        run: |
          reg add "HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\AppModelUnlock" /v AllowDevelopmentWithoutDevLicense /t REG_DWORD /d 1 /f

      - name: Build installer
        working-directory: desktop
        run: |
          npm run dist *>&1 | Tee-Object -FilePath build.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Print build log on failure
        if: failure()
        working-directory: desktop
        run: |
          Get-Content build.log -Tail 80
          $log = Get-Content build.log -Raw
          if ($log.Length -gt 18000) { $log = $log.Substring($log.Length - 18000) }
          $body = "DESKTOP BUILD FAILED at $(Get-Date -Format o)`n``````" + $log + "``````"
          gh issue create --title "desktop build error" --body $body || true

      - name: Upload to GitHub Releases
        if: success()
        working-directory: desktop
        run: |
          gh release create desktop --title "NSR Dashboard" --notes "لوحة تحكم سطح المكتب للبوت — أحدث إصدار" --latest=false || true
          gh release upload desktop dist/NSR-Dashboard-Setup-*.exe --clobber

      - name: Show artifacts
        working-directory: desktop
        run: Get-ChildItem dist | Select-Object Name, Length